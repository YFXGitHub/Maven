#配置连接池
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/day5
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: a123

#配置JPA
  jpa:
    hibernate:
      ddl-auto: update    #更新表， 没有则创建
    show-sql: true        #打印sql语句

#配置日志
logging:
  level:
    root: info